登录逻辑：
    接收参数：userAccount，userPassword
    请求类型：post(参数记录在请求体里，请求体格式为json，get不适用于请求体装载参数)
    返回值：用户信息(脱敏)
    逻辑：
        1.校验用户名和密码是否正确
            1.非空
            2.用户名长度在4位以上
            3.密码长度在8位以上
            4.账号不能包含特殊字符
        2.和数据库中的密文密码进行比对
        3.用户信息脱敏，隐藏用户敏感信息
        4.建立用户的session(登录态)，将其存到服务器上(用后端Spring框架的tomcat来实现session的存储)cookie
        5.返回用户信息(脱敏)
